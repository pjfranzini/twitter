<h1>User Profile for <%= @user.handle %></h1>

<p><%= @user.name %></p>
<p><%= @user.email %></p>

<h2><%= @user.handle %>'s Tweets</h2>

<p><%= image_tag(@user.avatar_url) if @user.avatar? %></p>
<ul>
  <% @tweets.each do |tweet| %>
    <li><%= tweet.content %></li>
  <% end %>
</ul>